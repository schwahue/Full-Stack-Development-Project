const Sequelize = require('sequelize');
const db = require('../config/DBConfig');

/* for sale records */

/* saleId, merchantID, itemID, quantity, price, review(??) */

/* when buyer review purchased item; update review score to acording number out of 5*/